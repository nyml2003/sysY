version: '1.0.0'
services:
  env:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["/bin/bash", "-c", "/tmp/sed/start.sh"]
    ports:
      - "8022:22"
    tty: true
    stdin_open: true
    volumes:
      - .:/tmp/sed
    
    