version: '1.0.0'
services:
  env:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["/bin/bash", "-c", "/app/start.sh"]
    ports:
      - "8022:22"
    tty: true
    stdin_open: true
    volumes:
      - .:/app
    
    